# Project Lens — Python dependencies
# Used by lens-forensic-report.yml (Anthropic API, python-docx)
# LENS-022: ref system merged into lens-manage-analyze.yml

#
# Other workflows (lens-collect, lens-gdelt, lens-manage-analyze)
# still use inline pip install — migrate in LENS-015.

anthropic
supabase
python-dotenv
requests
openpyxl
tzdata
python-docx==1.1.2
cohere
