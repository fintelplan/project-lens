# Project Lens Diary — Apr 30, 2026

## LENS-021 — Entity Registry Wired (Opus 4.7, T2 + LENS-021)

**Session window**: ~14:00 → 16:30 Thai (~2.5h)
**Commits**: 1 (`49c46f6`)
**Model**: Claude Opus 4.7 adaptive

**Started** with verification of S2-F detection pipeline (T2 from yesterday's marathon brief).
Confirmed `lens_operation_detections` schema live and writer fires. Manual `workflow_dispatch`
ran clean (39s, green ✅). Found GitHub Actions cron flakiness — only 2 of 4 expected runs
in 24h. Schedule itself is correct (`'30 13 * * *'` + `'30 1 * * *'`). GitHub silently skips
some scheduled fires; not a code bug.

**Discovered** silent data loss bug: 3 S2-F aggregators (Watch/Clarity/Verification) all
hard-coded `entity_id=None` while `lens_drift_findings` schema enforces NOT NULL. Every
finding write was failing silently with `null value in column "entity_id"` constraint
violation. TODO from LENS-020 was never closed.

**Fixed** as LENS-021:
- Expanded `lens_entities_entity_type_check` constraint to include `'state_office'`
- Seeded 3 rows: `trump_office`, `xi_office`, `khamenei_office`
- New `code/lens_s2f_helpers.py` — `get_state_office_entity_id(client, lens)` cached
- Patched 3 aggregators

**Pattern Match Bias incident**: jumped to ALTER TABLE recommendation before reading
the 2 other affected aggregators or verifying `lens_entity_extract.py` infrastructure
(already fully built since LENS-018). Operator corrected twice. Recovery only after
second push-back. Per GNI-R-233 — documented and added to memory.

**Lesson**: Opus 4.7 is overfitting to Project Lens lore. Spending 80% weekly limit on
~2.5h of work that's Sonnet-zone. Decision: switch to Sonnet 4.6 adaptive from LENS-022
forward; reserve Opus 4.7 for S4-B architecture (July) and Direction A web app.

**Closed pending**: tonight's scheduled cron (01:30 UTC May 1) is the real test —
if findings rows write with populated `entity_id`, fix is confirmed. Forensic Report
at 02:00 UTC will deliver tomorrow's docx with non-empty Part B (drift findings).

**Next session**: LENS-022 with Sonnet 4.6 adaptive. T1 Opus Report rewire is highest
priority once T2 verified.

---

**Entry written**: 16:30 Thai, Apr 30 2026 by Opus 4.7
