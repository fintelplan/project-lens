# NEXT SESSION BRIEF — LENS-047 (written at the LENS-046 close, 2026-09-24)

Project Lens — Bro Alpha. You are Claude. Bro Alpha calls you "my buddy". Speak Burmese if he opens in
Burmese. Read this, then `LENS_TARGET_AND_ORDER_LENS047.md` (the live order), then the completion test.

## 1. Open (bytes before anything)

1. `printf '\e[?2004l'`, then verify HEAD against `git ls-remote origin refs/heads/main` and the sha256 of
   the two close docs Bro Alpha attaches against `docs/`.
2. Expected HEAD: the LENS-046 close-docs commit on top of `1c52063`.
3. **Before any piece of work, look for the guideline or standard that applies** (Bro Alpha, widened at
   LENS-046). Web-search it; do not quote standard numbers from memory.

## 2. Where Lens stands

- **Target changed (D1 = A):** Lens earns its public voice (PHI-004 Phase 2), declared only by Bro Alpha.
  Three layers: Air & voice (bytes, every wave), Product truth (ICD 203), Freedom from Fear (PHI-004 + WHY).
  Layer 1 is green; Layers 2 and 3 are mostly red. Six-week clock from 2026-09-24; earliest go-live Nov 5.
- **The WHY** is written into canon for the first time: `LENS_FOUNDATIONS_LENS046.md` section 0.
- **Shipped at LENS-046:** CC-117 `3b103c8` (S2 Shaping Report restored — it had failed silently on every
  wave for ten days), CC-118 `309a7c8` (S3 message: hypotheses, not forecasts; "0 of N checked"),
  CC-119 `9cabfa7` (S3 prompts ask for the cause already in motion with a confirming and a disconfirming
  sign), `1c52063` (identity separation of the public tree). Gates now 31.
- **Found and recorded, not built:** S4 has never checked a prediction (0 of 112); the MA threat level
  flips every wave; S2-F re-sends five findings each wave; the S4 rating tool writes columns that do not
  exist; raw markdown and mid-word cuts in six message kinds. All are rows in the completion test.

## 3. First job: read the certs (order item 3)

- CC-118 and CC-119: the newest Manage + Analyze run whose head descends from `9cabfa7`
  (`git merge-base --is-ancestor 9cabfa7 <sha>`); grep its log for `CERTAINTY_LANGUAGE`,
  `check count unavailable`, `send_s3_intelligence failed`; read the newest `lens_predictions` row.
  Then ask Bro Alpha for the S3 Telegram message (screenshot).
- CC-117: the S2 docx itself (headings, subtitle, no markdown marks).
- CC-108: S3-C runs weekly, not on Thursdays — read it on its next run.

## 4. Mission (proposal; Bro Alpha declares it)

L3.4 — send one S2-F finding once until it changes. Then L3.2 — the threat level moves only with its
evidence and says why. Ruling 5 in the completion test gives the full order.

## 5. Cautions carried from LENS-046

- **Identity:** nothing but "Bro Alpha" for the operator; the partner project only as "Partner A".
  Before any commit, scan the staged index case-insensitively for the old identifiers (the gate is in the
  LENS-046 record) — file names, metadata and words split across lines included. Never `git add -A`.
- The public order does not describe the remaining identity steps on purpose; Bro Alpha keeps them
  off-repo.
- Commit messages: `-F` with a quoted heredoc, written outside any conditional.
- Line endings differ per file (LF and CRLF both live); patch per file's own ending.
- A zero from a search API proves nothing without a positive control.
- The weekly review is Bro Alpha's to judge; Claude lays out the evidence.

## 6. Session record

LENS-046 ran Sep 23-24 in Burmese: 4 code commits, one tree-wide de-identification, 5 rulings on the
completion test, Review #1, the root-cause analysis (validation was the missing phase), and the lineage
of "inevitable" vs "never predict" (born together in S3-A's prompt at LENS-010; obeyed only after the
August model change).
